//conectarse a la base de datos
const pool = require("../db");

//Crear empleado, Generar Reportes, Control Tarjetas

const getAllEmployed = async (req, res, next) =>{
    try {
        
    } catch (error) {
        next(error);
    }
}